{
  "$schema": "https://turbo.build/schema.json",
  "extends": [],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "cache": true,
      "dependsOn": ["^build"]
    },
    "start:dev": {
      "dependsOn": ["^build"]
    }
  }
}
